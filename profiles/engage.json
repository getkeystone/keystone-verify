{
  "name": "keystone-engage",
  "description": "Keystone Engage: governed conversational agent for regulated customer interaction.",
  "base_url": "http://localhost:8100",
  "endpoint": "/engage",
  "method": "POST",
  "timeout_s": 60,
  "response_mapping": {
    "answer_field": "message",
    "severity_field": "severity",
    "citations_field": "evidence",
    "audit_hash_field": "audit_hash",
    "fail_closed_field": null
  }
}
